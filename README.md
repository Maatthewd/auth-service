ADMIN:

GET /admin/users — listar usuarios (READ_USERS)

POST /admin/users — crear usuarios (CREATE_USERS)

PUT /admin/users/{id} — modificar usuarios (UPDATE_USERS)

DELETE /admin/users/{id} — borrar usuarios (DELETE_USERS)

USUARIO:

GET /me — obtener perfil (READ_SELF)

PUT /me — editar perfil (UPDATE_SELF)

DELETE /me — borrar mi cuenta (DELETE_SELF)



Soporte / Auditoría (Opcionales)

GET /logs

GET /audit